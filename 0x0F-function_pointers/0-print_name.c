#include "function_pointers.h"
#include "stdlibe.h"
/**
 *print_name - prints a name
 *@name: string
 *@f: function
 */
void print_name(char *name, void (*f)(char *))
{
if (!name || if)
return;
f(name);
}
